name: RDP

on:
  workflow_dispatch:
    inputs:
      rdp_username:
        description: 'RDP 用户名（可选，如不输入将使用 DEFAULT_RDP_USERNAME 变量）'
        required: false
        default: ''
        type: string

jobs:
  secure-rdp:
    runs-on: windows-latest
    timeout-minutes: 360

    steps:
      - name: Configure Core RDP Settings
        run: |
          # Enable Remote Desktop and disable Network Level Authentication (if needed)
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server' `
                             -Name "fDenyTSConnections" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "UserAuthentication" -Value 0 -Force
          Set-ItemProperty -Path 'HKLM:\System\CurrentControlSet\Control\Terminal Server\WinStations\RDP-Tcp' `
                             -Name "SecurityLayer" -Value 0 -Force

          # Remove any existing rule with the same name to avoid duplication
          netsh advfirewall firewall delete rule name="RDP-Tailscale"
          
          # For testing, allow any incoming connection on port 3389
          netsh advfirewall firewall add rule name="RDP-Tailscale" `
            dir=in action=allow protocol=TCP localport=3389

          # (Optional) Restart the Remote Desktop service to ensure changes take effect
          Restart-Service -Name TermService -Force

      - name: Determine RDP Username
        run: |
          # 方法1：优先使用手动输入的用户名
          $manualUsername = "${{ github.event.inputs.rdp_username }}"
          
          # 方法2：如果没有手动输入，使用 Variables 中的预设用户名
          $presetUsername = "${{ vars.DEFAULT_RDP_USERNAME }}"
          
          # 方法3：如果都没有设置，使用默认用户名
          if (-not [string]::IsNullOrWhiteSpace($manualUsername)) {
              Write-Host "使用手动输入的用户名"
              $username = $manualUsername
              $usernameSource = "手动输入"
          } elseif (-not [string]::IsNullOrWhiteSpace($presetUsername)) {
              Write-Host "使用 Variables 中预设的用户名"
              $username = $presetUsername
              $usernameSource = "Variables 预设"
          } else {
              Write-Host "使用默认用户名"
              $username = "RDPUser"
              $usernameSource = "默认"
          }
          
          Write-Host "最终用户名: $username"
          Write-Host "用户名来源: $usernameSource"
          
          # 将用户名信息存储到环境变量
          echo "RDP_USER=$username" >> $env:GITHUB_ENV
          echo "RDP_USER_SOURCE=$usernameSource" >> $env:GITHUB_ENV

      - name: Create RDP User with Flexible Password
        run: |
          # 检查用户是否已存在，如果存在则删除
          if (Get-LocalUser -Name $env:RDP_USER -ErrorAction SilentlyContinue) {
              Write-Host "用户 $env:RDP_USER 已存在，删除旧用户"
              Remove-LocalUser -Name $env:RDP_USER -Force
          }
          
          # 方法1：优先使用 GitHub Secrets 中的密码
          $passwordFromSecret = "${{ secrets.RDP_PASSWORD }}"
          
          # 方法2：如果设置了 USE_RANDOM_PASSWORD 变量，使用随机密码
          $useRandom = "${{ vars.USE_RANDOM_PASSWORD }}"
          
          if (-not [string]::IsNullOrWhiteSpace($passwordFromSecret)) {
              Write-Host "使用 GitHub Secrets 中设置的密码"
              $password = $passwordFromSecret
              $passwordSource = "GitHub Secrets"
          } elseif ($useRandom -eq "true" -or $useRandom -eq "1") {
              Write-Host "使用随机生成的密码"
              # 生成包含大小写字母、数字和特殊字符的随机密码
              $charSet = @{
                  Upper   = [char[]](65..90)      # A-Z
                  Lower   = [char[]](97..122)     # a-z
                  Number  = [char[]](48..57)      # 0-9
                  Special = ([char[]](33..47) + [char[]](58..64) +
                             [char[]](91..96) + [char[]](123..126)) # 特殊字符
              }
              $rawPassword = @()
              $rawPassword += $charSet.Upper | Get-Random -Count 3
              $rawPassword += $charSet.Lower | Get-Random -Count 3
              $rawPassword += $charSet.Number | Get-Random -Count 3
              $rawPassword += $charSet.Special | Get-Random -Count 3
              $password = -join ($rawPassword | Sort-Object { Get-Random })
              $passwordSource = "随机密码"
          } else {
              Write-Host "使用固定密码"
              $password = "Aa147369"
              $passwordSource = "固定密码"
          }
          
          Write-Host "用户名: $env:RDP_USER"
          Write-Host "用户名来源: $env:RDP_USER_SOURCE"
          Write-Host "密码来源: $passwordSource"
          Write-Host "密码: $password"
          Write-Host "密码长度: $($password.Length) 位"
          
          $securePass = ConvertTo-SecureString $password -AsPlainText -Force
          New-LocalUser -Name $env:RDP_USER -Password $securePass -AccountNeverExpires
          Add-LocalGroupMember -Group "Administrators" -Member $env:RDP_USER
          Add-LocalGroupMember -Group "Remote Desktop Users" -Member $env:RDP_USER
          
          # 将密码信息存储到环境变量
          echo "RDP_PASSWORD_SOURCE=$passwordSource" >> $env:GITHUB_ENV
          echo "RDP_PASSWORD_VALUE=$password" >> $env:GITHUB_ENV
          
          if (-not (Get-LocalUser -Name $env:RDP_USER)) {
              Write-Error "用户创建失败"
              exit 1
          }

      - name: Install Tailscale
        run: |
          $tsUrl = "https://pkgs.tailscale.com/stable/tailscale-setup-latest-amd64.msi"
          $installerPath = "$env:TEMP\tailscale.msi"
          
          Invoke-WebRequest -Uri $tsUrl -OutFile $installerPath
          Start-Process msiexec.exe -ArgumentList "/i", "`"$installerPath`"", "/quiet", "/norestart" -Wait
          Remove-Item $installerPath -Force

      - name: Establish Tailscale Connection
        run: |
          # Bring up Tailscale with the provided auth key from secrets
          & "$env:ProgramFiles\Tailscale\tailscale.exe" up --authkey=${{ secrets.TAILSCALE_AUTH_KEY }} --hostname=gh-runner-$env:GITHUB_RUN_ID
          
          # Wait for Tailscale to assign an IP
          $tsIP = $null
          $retries = 0
          while (-not $tsIP -and $retries -lt 10) {
              $tsIP = & "$env:ProgramFiles\Tailscale\tailscale.exe" ip -4
              Start-Sleep -Seconds 5
              $retries++
          }
          
          if (-not $tsIP) {
              Write-Error "Tailscale IP 未分配，退出"
              exit 1
          }
          echo "TAILSCALE_IP=$tsIP" >> $env:GITHUB_ENV
      
      - name: Verify RDP Accessibility
        run: |
          Write-Host "Tailscale IP: $env:TAILSCALE_IP"
          
          # Test connectivity using Test-NetConnection against the Tailscale IP on port 3389
          $testResult = Test-NetConnection -ComputerName $env:TAILSCALE_IP -Port 3389
          if (-not $testResult.TcpTestSucceeded) {
              Write-Error "TCP 连接到 RDP 端口 3389 失败"
              exit 1
          }
          Write-Host "TCP 连接测试成功！"

      - name: Display Connection Information
        run: |
          Write-Host "`n=== RDP 连接信息 ==="
          Write-Host "地址: $env:TAILSCALE_IP"
          Write-Host "用户名: $env:RDP_USER"
          Write-Host "用户名来源: $env:RDP_USER_SOURCE"
          Write-Host "密码: $env:RDP_PASSWORD_VALUE"
          Write-Host "密码来源: $env:RDP_PASSWORD_SOURCE"
          Write-Host "密码长度: $($env:RDP_PASSWORD_VALUE.Length) 位"
          Write-Host "===================`n"
          
          if ($env:RDP_PASSWORD_SOURCE -eq "固定密码") {
              Write-Host "提示: 当前使用固定密码，建议："
              Write-Host "1. 在 GitHub Secrets 中设置 RDP_PASSWORD"
              Write-Host "2. 或在 GitHub Variables 中设置 USE_RANDOM_PASSWORD=true"
          } elseif ($env:RDP_PASSWORD_SOURCE -eq "随机密码") {
              Write-Host "提示: 每次运行都会生成新的随机密码，请妥善保存当前密码"
          }

      - name: Maintain Connection
        run: |
          Write-Host "`n=== RDP 服务运行中 ==="
          Write-Host "连接地址: $env:TAILSCALE_IP"
          Write-Host "用户名: $env:RDP_USER"
          Write-Host "用户名来源: $env:RDP_USER_SOURCE"
          Write-Host "密码: $env:RDP_PASSWORD_VALUE"
          Write-Host "密码来源: $env:RDP_PASSWORD_SOURCE"
          Write-Host "===================`n"
          Write-Host "提示: 在 GitHub Actions 界面点击 'Cancel workflow' 可终止 RDP 服务"
          
          # 时间提醒计数器
          $counter = 0
          $reminderShown = @()
          
          # Keep runner active indefinitely (or until manually cancelled)
          while ($true) {
              $counter++
              $minutesRunning = $counter * 5
              
              # 时间提醒
              if ($minutesRunning -eq 60 -and "1hour" -notin $reminderShown) {
                  Write-Host "⏰ 提醒: 已运行1小时，剩余5小时"
                  $reminderShown += "1hour"
              }
              elseif ($minutesRunning -eq 180 -and "3hour" -notin $reminderShown) {
                  Write-Host "⏰ 提醒: 已运行3小时，剩余3小时"
                  $reminderShown += "3hour"
              }
              elseif ($minutesRunning -eq 300 -and "5hour" -notin $reminderShown) {
                  Write-Host "🚨 提醒: 剩余1小时，请保存工作！"
                  $reminderShown += "5hour"
              }
              
              # 定期状态显示（每30分钟显示完整信息包括密码）
              if ($counter % 6 -eq 0) {
                  Write-Host "`n[$(Get-Date)] RDP 服务正常运行中..."
                  Write-Host "地址: $env:TAILSCALE_IP"
                  Write-Host "用户名: $env:RDP_USER (来源: $env:RDP_USER_SOURCE)"
                  Write-Host "密码: $env:RDP_PASSWORD_VALUE"
                  Write-Host "运行时间: $minutesRunning 分钟"
                  Write-Host "使用 Ctrl+C 或点击 Cancel workflow 终止服务`n"
              } else {
                  Write-Host "[$(Get-Date)] RDP Active - 运行时间: $minutesRunning 分钟"
              }
              Start-Sleep -Seconds 300
          }
